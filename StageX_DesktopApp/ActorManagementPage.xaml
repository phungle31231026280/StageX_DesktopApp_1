<Page x:Class="StageX_DesktopApp.ActorManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Diễn viên"
      Background="#FF1A1A1A" Foreground="White"
      Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="ButtonAddStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF198754"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="ButtonEditStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF0D6EFD"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,3"/>
        </Style>
        <Style x:Key="ButtonDeleteStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFDC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,3"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#FF333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="QUẢN LÝ DIỄN VIÊN" FontSize="24" FontWeight="Bold" Foreground="#FFffc107" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FF0C1220" CornerRadius="8" Padding="20" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Thêm / Sửa Diễn viên" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <TextBox x:Name="ActorIdBox" Visibility="Collapsed"/>

                    <TextBlock Text="Họ và Tên:" Foreground="Gray" Margin="0,0,0,5"/>
                    <TextBox x:Name="FullNameBox"/>

                    <TextBlock Text="Nghệ danh (Nick name):" Foreground="Gray" Margin="0,10,0,5"/>
                    <TextBox x:Name="NickNameBox"/>

                    <TextBlock Text="Link Ảnh (Avatar URL):" Foreground="Gray" Margin="0,10,0,5"/>
                    <TextBox x:Name="AvatarUrlBox"/>
                    <Image x:Name="PreviewImage" Height="100" Width="100" Margin="0,10,0,0" HorizontalAlignment="Left" Stretch="UniformToFill"/>

                    <TextBlock Text="Trạng thái:" Foreground="Gray" Margin="0,10,0,5"/>
                    <ComboBox x:Name="StatusComboBox" Padding="8" SelectedIndex="0">
                        <ComboBoxItem Content="Hoạt động"/>
                        <ComboBoxItem Content="Ngừng hoạt động"/>
                    </ComboBox>

                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                        <Button x:Name="SaveButton" Content="Lưu Diễn viên" Style="{StaticResource ButtonAddStyle}" Click="SaveButton_Click"/>
                        <Button x:Name="ClearButton" Content="Hủy" Background="Gray" Foreground="White" Padding="10,5" Margin="10,0,0,0" Click="ClearButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox x:Name="SearchBox" Width="250" Tag="Tìm theo tên..." KeyUp="SearchBox_KeyUp"/>
                    <Button Content="Tìm kiếm" Background="#FF0D6EFD" Foreground="White" Padding="10,5" Margin="10,0,0,0" Click="SearchButton_Click"/>
                </StackPanel>

                <DataGrid Grid.Row="1" x:Name="ActorsGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                          Background="Transparent" RowBackground="#FF1E1E1E" Foreground="White" HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#333">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding ActorId}" Width="50"/>

                        <DataGridTemplateColumn Header="Avatar" Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding AvatarUrl}" Width="40" Height="40" Stretch="UniformToFill">
                                        <Image.Clip>
                                            <EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/>
                                        </Image.Clip>
                                    </Image>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Họ Tên" Binding="{Binding FullName}" Width="*"/>
                        <DataGridTextColumn Header="Nghệ danh" Binding="{Binding NickName}" Width="*"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="Auto"/>

                        <DataGridTemplateColumn Header="Hành động" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Sửa" Style="{StaticResource ButtonEditStyle}" Margin="0,0,5,0" Click="EditButton_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</Page>