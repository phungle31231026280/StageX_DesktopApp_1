<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="StageX_DesktopApp.DashboardPage"
      Title="Bảng điều khiển"
      Background="#FF1A1A1A"
      Foreground="White"
      Loaded="Page_Loaded" Height="622" Width="940">
    <!--
        Trang Dashboard hiển thị số liệu thống kê cho quản trị viên.
        Bốn thẻ tổng quan ở phía trên cho biết tổng doanh thu, số đơn hàng, số vở diễn và số thể loại.
        Phía dưới là hai biểu đồ: doanh thu theo tháng và số lượng vé bán với bộ lọc thời gian (ngày/tuần/tháng/năm).
        Sau cùng là bảng Top 5 vở diễn bán chạy nhất. Tất cả dữ liệu được lấy thông qua các thủ tục lưu trữ.
    -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Vùng thẻ tổng quan -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Doanh thu -->
            <Border Grid.Column="0" Background="#FF0C1220" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Tổng doanh thu" Foreground="#FFFFC107" FontSize="14"/>
                    <TextBlock x:Name="RevenueTotalText" Text="0đ" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Đơn hàng -->
            <Border Grid.Column="1" Background="#FF0C1220" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Đơn hàng" Foreground="#FFFFC107" FontSize="14"/>
                    <TextBlock x:Name="OrderTotalText" Text="0" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Vở diễn -->
            <Border Grid.Column="2" Background="#FF0C1220" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Vở diễn" Foreground="#FFFFC107" FontSize="14"/>
                    <TextBlock x:Name="ShowTotalText" Text="0" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Thể loại -->
            <Border Grid.Column="3" Background="#FF0C1220" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Thể loại" Foreground="#FFFFC107" FontSize="14"/>
                    <TextBlock x:Name="GenreTotalText" Text="0" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Biểu đồ -->
        <Grid Margin="2,104,0,215" Grid.RowSpan="3">
            <!-- Thêm một cột nữa cho biểu đồ đánh giá sao -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="209*"/>
                <ColumnDefinition Width="29*"/>
                <ColumnDefinition Width="62*"/>
                <ColumnDefinition Width="104*"/>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="98*"/>
                <ColumnDefinition Width="15*"/>
                <ColumnDefinition Width="43*"/>
                <ColumnDefinition Width="13*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="299*"/>
            </Grid.ColumnDefinitions>

            <!-- Biểu đồ doanh thu -->
            <Border Grid.Column="0" Background="#FF0C1220" CornerRadius="8" Padding="10" Margin="5,-5,5,5" Grid.ColumnSpan="3">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Doanh thu theo tháng" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFC107" Margin="0,-5,0,5"/>
                    <!-- Biểu đồ doanh thu sử dụng LiveCharts -->
                    <lvc:CartesianChart x:Name="RevenueChart" Grid.Row="1" Margin="0,10,0,0" Background="Transparent"/>
                </Grid>
            </Border>

            <!-- Biểu đồ vé bán -->
            <Border Grid.Column="3" Background="#FF0C1220" CornerRadius="8" Padding="10" Margin="5,-4,0,5" Grid.ColumnSpan="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!-- Tiêu đề và bộ lọc -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Số lượng vé đã bán" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFC107" Grid.ColumnSpan="2" Margin="0,-4,87,4"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="1,26,0,-26">
                            <RadioButton x:Name="DayFilterButton" Content="Ngày" IsChecked="True" GroupName="TicketFilter" Margin="5,0" Foreground="White" Checked="FilterChanged"/>
                            <RadioButton x:Name="WeekFilterButton" Content="Tuần" GroupName="TicketFilter" Margin="5,0" Foreground="White" Checked="FilterChanged"/>
                            <RadioButton x:Name="MonthFilterButton" Content="Tháng" GroupName="TicketFilter" Margin="5,0" Foreground="White" Checked="FilterChanged"/>
                            <RadioButton x:Name="YearFilterButton" Content="Năm" GroupName="TicketFilter" Margin="5,0" Foreground="White" Checked="FilterChanged"/>
                        </StackPanel>
                    </Grid>
                    <!-- Biểu đồ số vé sử dụng LiveCharts -->
                    <lvc:CartesianChart x:Name="TicketsChart" Grid.Row="1" Margin="0,23,7,0" Background="Transparent"/>
                </Grid>
            </Border>

            <!-- Biểu đồ đánh giá sao dạng donut -->
            <Border Grid.Column="11" Background="#FF0C1220" CornerRadius="8" Padding="10" Margin="5,-1,4,5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Đánh giá theo sao" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFC107" Margin="0,-10,0,10"/>
                    <!-- PieChart (donut) từ LiveCharts -->
                    <lvc:PieChart x:Name="RatingChart" Grid.Row="1" Margin="0,10,0,0" Background="Transparent"
                                  InnerRadius="50" LegendLocation="Bottom"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Bảng top 5 -->
        <Border Grid.Row="2" Background="#FF0C1220" CornerRadius="8" Padding="10" Margin="5,259,5,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="483*"/>
                    <ColumnDefinition Width="42*"/>
                    <ColumnDefinition Width="281*"/>
                    <ColumnDefinition Width="64*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="TOP 5 vở diễn bán chạy nhất" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFC107" Margin="-1,8,1,139" Grid.Row="1" Grid.ColumnSpan="4"/>
                <DataGrid Grid.Row="1" x:Name="TopShowsGrid" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" LoadingRow="TopShowsGrid_LoadingRow" RowBackground="#00000000" AlternatingRowBackground="#22000000" Margin="-1,38,0,0" Grid.ColumnSpan="4">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT" Binding="{Binding Index}" Width="50"/>
                        <DataGridTextColumn Header="Tên vở diễn" Binding="{Binding show_name}" Width="2*"/>
                        <DataGridTextColumn Header="Số vé đã bán" Binding="{Binding sold_tickets}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>